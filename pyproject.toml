[project]
name = "genai-demo"
version = "0.1.0"
description = "Tiny educational demo of the process behind generative AI / LLMs"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.2",
    "transformers>=4.40",
    "datasets",
    "pandas",
    "matplotlib",
    "seaborn",
    "scikit-learn",
    "huggingface_hub",
    "openpyxl",
    "requests",
    "python-dotenv",
]
